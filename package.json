{
  "name": "vecline-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Vecline Studio — the free, private, in-browser image to vector studio. Runs entirely on the client; nothing is uploaded.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "serve": "node scripts/serve.mjs",
    "start": "npm run build && npm run serve",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "vecline": "^1.36.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.7.2"
  }
}
